"""Manual (in-game) validation tests for Metroid Prime 2: Echoes.

Each ``mtNN_*.py`` module defines one ``TEST = harness.ManualTest(...)`` and
builds a purpose-made ISO that makes exactly one in-game fact cheap to
observe. See ``MANUAL_TEST_PLAN.md`` and ``README.md`` in this package.
"""
