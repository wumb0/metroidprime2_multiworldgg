# Metroid Prime 2: Echoes -- manual (in-game) test book

GENERATED by `build_readme.py` from every `mt*.py` module's `TEST` spec --
do not edit by hand. Regenerate with:

```
cd MultiWorldGG
SKIP_REQUIREMENTS_UPDATE=1 .venv/bin/python -m worlds.metroidprime2.test.manual.build_readme
```

`MANUAL_TEST_PLAN.md` is the design; this is the runnable book it produces.

## Prerequisites

* A MultiWorldGG source checkout (this repo) with its venv, and this world's
  symlink in place (`MultiWorldGG/worlds/metroidprime2 -> metroidprime2`).
* A vanilla **NTSC-U** Metroid Prime 2: Echoes ISO (and ideally a **PAL** one
  for `MT13`). NKit/RVZ/WIA/GCZ/CISO images are not supported.
* Dolphin with the memory engine reachable, and `host.yaml`'s
  `metroidprime2_options.rom_file` / `emulator_settings.executable_path` set.
* `ppc-asm>=1.9.0` installed (host generation imports it via the patch
  pipeline); `SKIP_REQUIREMENTS_UPDATE=1` keeps world auto-discovery from
  pip-installing every other world's requirements.

## How to run any test

```
1. Build      python -m worlds.metroidprime2.test.manual.<slug> --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/<slug>/<slug>.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/<slug>/<slug>.apmp2 <vanilla.iso>
4. Steps      follow the section below
5. Pass if    every pass criterion below holds
6. If it fails, inspect the files named under "If it fails, look at"
```

Shared flags: `--iso PATH`, `--pal`, `--out DIR`, `--clean`, `--no-patch`,
`--verify`, `--repin`, `--print-readme`, `--variant <name>`.

`--verify` builds and checks the pinned `config.json` hash without patching;
`--repin` prints the new hash to paste into the test module when a deliberate
change moved it. `client.py` has no `__main__` guard, so it is started
through the Launcher, not `python -m`; the ISO is already patched by step 1,
so the client reuses it rather than re-patching.

## Results table to copy

| ID | Date | ISO version | Result | Notes |
|---|---|---|---|---|
| MT01 | | NTSC / PAL | pass / fail | |
| MT02 | | | | |
| MT03 | | | | |
| MT04 | | | | |
| MT05 | | | | |
| MT06 | | | | |
| MT07 | | | | |
| MT08 | | | | |
| MT09 | | | | |
| MT10 | | | | |
| MT11 | | | | |
| MT12 | | | | |
| MT13 | | | | |

## Tests

### MT01_SMOKE_VANILLA -- `mt01_smoke_vanilla` (P0)

*Proves: a default-options patched ISO boots, starts, saves, and reports a check*

Build:
```
python -m worlds.metroidprime2.test.manual.mt01_smoke_vanilla --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt01_smoke_vanilla --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt01_smoke_vanilla/mt01_smoke_vanilla.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt01_smoke_vanilla/mt01_smoke_vanilla.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Boot the patched ISO in Dolphin.
   **Expect:** The game reaches the title screen; New Game works.
2. **Do:** Start New Game and skip the ship cutscene with Start.
   **Expect:** You spawn at the vanilla Landing Site save station.
3. **Do:** Walk to Hive Chamber A and collect the pickup there.
   **Expect:** The game plays the pickup animation and the client logs the location check; the server shows the check, and the HUD memo names the item.
4. **Do:** Return to the ship and save at the save station.
   **Expect:** The save completes normally.
5. **Do:** Quit to the main menu and load the save.
   **Expect:** The save loads and the pickup is still collected.

Pass if:
* The ISO boots and reaches gameplay with no crash.
* Hive Chamber A's check is reported exactly once (no duplicate location check).
* The in-game HUD memo matches the item's name.
* Saving, quitting, and loading works.

If it fails, look at:
* `client/patcher_runner.py` (the DOL writes and the patch pipeline)
* `patch_data.py` (the generated `config.json`)
* the pinned open-prime-rando version (`utils.OPEN_PRIME_RANDO_VERSION`)

### MT02_GOAL_CREDITS -- `mt02_goal_credits` (P0)

*Proves: beating the game reaches Credits and the client's current-area memory read marks the goal*

Build:
```
python -m worlds.metroidprime2.test.manual.mt02_goal_credits --iso <vanilla.iso>
```

What the build contains:
* starting room: `Sky Temple/Sanctum/Door to Sanctum Access`
* options: `defense_up_damage_reduction=90`, `door_lock_rando=False`, `double_damage_multiplier=500`, `elevator_rando=False`, `energy_per_tank=500`, `map_visibility=full_map_and_items`, `portal_rando=False`, `sky_temple_keys=9`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x2, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x28, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1

Notes / derived values:
* The Credits room is `!!game_end_part3` (randovania Temple Grounds/Credits asset id `constants.CREDITS_MREA`); its TAreaId index is in `constants.GAME_END_AREA_INDICES`. Detection is `game_interface.current_mlvl()` == `constants.TEMPLE_GROUNDS_MLVL` plus `game_interface.current_area_id()` in that set -- no ISO patch involved.

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt02_goal_credits --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt02_goal_credits/mt02_goal_credits.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt02_goal_credits/mt02_goal_credits.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Start New Game.
   **Expect:** You spawn in the Emperor Ing arena (Sky Temple/Sanctum).
2. **Do:** Kill Emperor Ing.
   **Expect:** The boss dies; the escape sequence begins.
3. **Do:** Complete the escape sequence and the Dark Samus 3 & 4 fight at Sky Temple Gateway.
   **Expect:** The Credits area loads.
4. **Do:** Let the Credits area load for at least one client tick (0.5s).
   **Expect:** The client logs the goal, sends StatusUpdate(GOAL), and the server marks the slot finished (`!status` shows goal). The player never triggers a spurious location check.

Pass if:
* The goal is reported exactly once.
* No spurious location checks are sent around the Credits load.
* No client traceback.

If it fails, look at:
* `constants.GAME_END_AREA_INDICES` / `constants.TEMPLE_GROUNDS_MLVL`
* `client/game_interface.py::current_area_id` / `current_mlvl`
* `client/versions.py` AREA_ID_OFFSET for this build
* `client/client.py::_handle_check_goal`

### MT03_GOAL_AREA_POKE -- `mt03_goal_area_poke` (P0)

*Proves: the goal *detector* fires off a real memory read on this build, without the final boss*

Build:
```
python -m worlds.metroidprime2.test.manual.mt03_goal_area_poke --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`

Notes / derived values:
* NTSC: the current area's TAreaId lives at `0x803DB6E0 + 0x16A0` (write a u32 `53`, a member of `constants.GAME_END_AREA_INDICES`).
* PAL: the current area's TAreaId lives at `0x803DC900 + 0x16A0` (write a u32 `53`, a member of `constants.GAME_END_AREA_INDICES`).

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt03_goal_area_poke --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt03_goal_area_poke/mt03_goal_area_poke.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt03_goal_area_poke/mt03_goal_area_poke.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Build (any seed works) and start any game in a Temple Grounds room (e.g. Landing Site).
   **Expect:** You are in-game with the client connected; the current MLVL is Temple Grounds.
2. **Do:** Open Dolphin's Memory viewer and follow the address above for your ISO version.
   **Expect:** You find the live current-area TAreaId field.
3. **Do:** Write `53` to that field.
   **Expect:** Within one client tick the client logs the goal and sends StatusUpdate(GOAL); the server marks the slot finished.

Pass if:
* The goal is reported exactly once.
* No spurious location checks are sent.
* No client traceback.

If it fails, look at:
* `client/client.py::_handle_check_goal`
* `constants.GAME_END_AREA_INDICES`
* `client/versions.py` AREA_ID_OFFSET for this build

### MT04_WARP_TO_START_SAVE_STATION -- `mt04_warp_to_start_save_station` (P0)

*Proves: L+R decline warps; plain decline is vanilla; the prompt shows the hint; the target is the starting room*

Build:
```
python -m worlds.metroidprime2.test.manual.mt04_warp_to_start_save_station --iso <vanilla.iso>
```

What the build contains:
* starting room: `Temple Grounds/Hive Save Station/Save Station`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`, `warp_to_start=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x14, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt04_warp_to_start_save_station --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt04_warp_to_start_save_station/mt04_warp_to_start_save_station.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt04_warp_to_start_save_station/mt04_warp_to_start_save_station.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Spawn in Hive Save Station and activate the save station.
   **Expect:** The prompt text carries the extra line "Hold L + R while choosing No to warp to the starting room."
2. **Do:** Choose No *without* holding L + R.
   **Expect:** Bit-for-bit vanilla behavior: the no-save cinematic plays, no warp, no HUD memo.
3. **Do:** Choose No *with* L + R held.
   **Expect:** The HUD memo "Returning to starting room..." appears, then a room transition ~3s later back into this same room.
4. **Do:** Walk two rooms away, come back, and repeat the L+R decline.
   **Expect:** Same result: the layer is per-room, not a one-shot.
5. **Do:** Travel to a different save station (Temple Grounds/Landing Site/Save Station, the ship) and repeat the L+R decline.
   **Expect:** The warp returns you to Hive Save Station, not the ship.

Pass if:
* The hint line appears only when the warp option is on.
* Plain decline (no L+R) is unchanged from vanilla.
* L+R decline warps to the starting room every time, from every save station.
* `--variant off` produces no hint and no warp.

If it fails, look at:
* `client/warp_patch.py`
* `client/versions.py::WarpToStartAddresses`
* `tools/find_warp_addresses.py` (re-derive the hook address for this build)

### MT04_WARP_TO_START_SAVE_STATION -- `mt04_warp_to_start_save_station_off` (P0)

*Proves: L+R decline warps; plain decline is vanilla; the prompt shows the hint; the target is the starting room*

Build:
```
python -m worlds.metroidprime2.test.manual.mt04_warp_to_start_save_station --iso <vanilla.iso> --variant off
```

What the build contains:
* starting room: `Temple Grounds/Hive Save Station/Save Station`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`, `warp_to_start=False`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x14, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt04_warp_to_start_save_station --iso <vanilla.iso> --variant off
2. Host       python MultiServer.py manual_tests/mt04_warp_to_start_save_station_off/mt04_warp_to_start_save_station_off.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt04_warp_to_start_save_station_off/mt04_warp_to_start_save_station_off.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Spawn in Hive Save Station, activate it, and choose No with L + R held.
   **Expect:** No hint line and no warp: with `warp_to_start: false` the warp layer/hook are never installed.

Pass if:
* The hint line appears only when the warp option is on.
* Plain decline (no L+R) is unchanged from vanilla.
* L+R decline warps to the starting room every time, from every save station.
* `--variant off` produces no hint and no warp.

If it fails, look at:
* `client/warp_patch.py`
* `client/versions.py::WarpToStartAddresses`
* `tools/find_warp_addresses.py` (re-derive the hook address for this build)

### MT05_WARP_TO_START_REMOTE_ROOM -- `mt05_warp_to_start_remote_room` (P0)

*Proves: warp-to-start returns to the configured starting_area, even when it isn't a save station*

Build:
```
python -m worlds.metroidprime2.test.manual.mt05_warp_to_start_remote_room --iso <vanilla.iso>
```

What the build contains:
* starting room: `Temple Grounds/Windchamber Gateway/Door to Path of Eyes`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `starting_room=anywhere`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`, `warp_to_start=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x14, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt05_warp_to_start_remote_room --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt05_warp_to_start_remote_room/mt05_warp_to_start_remote_room.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt05_warp_to_start_remote_room/mt05_warp_to_start_remote_room.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Start New Game and note the room you spawn in.
   **Expect:** You spawn in Windchamber Gateway (a non-save-station room).
2. **Do:** Travel to the nearest save station and decline with L + R held.
   **Expect:** You land back in Windchamber Gateway, not the save station.

Pass if:
* The warp target is the forced starting room (Windchamber Gateway).
* The 18 save rooms are the only warp sources (you cannot warp from elsewhere).

If it fails, look at:
* `client/warp_patch.py` (`warp_patch.register` uses `configuration.starting_area`)
* `logic/regions.py::can_warp_to_start`

### MT06_ITEM_GRANT_CAPACITIES -- `mt06_item_grant_capacities` (P0)

*Proves: cross-computed capacities (ammo, launcher, progressives) land correctly in the live inventory*

Build:
```
python -m worlds.metroidprime2.test.manual.mt06_item_grant_capacities --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `energy_per_tank=250`, `map_visibility=full_map_and_items`, `progressive_grapple=True`, `progressive_suit=True`, `unvisited_room_names=True`

Notes / derived values:
* Non-tested items are granted via `start_inventory` so the plando bench can't make the seed unfillable; the tested items below are all collected in-game.

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt06_item_grant_capacities --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt06_item_grant_capacities/mt06_item_grant_capacities.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt06_item_grant_capacities/mt06_item_grant_capacities.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Build both variants side by side (`--variant a` and `--variant b`).
   **Expect:** See the two variant sections below for each one's collection order and expected capacity table.

Pass if:
* Variant a: expansions before the launcher grant capacity but no usable missiles until the launcher.
* Variant b: expansions unlock their weapon immediately.
* Progressive Suit grants Dark Suit then Light Suit; Progressive Grapple grants Grapple then Screw Attack.
* Energy Tanks are worth 250 each.
* The server-console expansion pushes capacity up by exactly one expansion.

If it fails, look at:
* `client/receive_items.py` (`compute_desired_capacities`/`plan_grants`)
* `client/client.py::_handle_grant_items`
* `patch_data.starting_items_config` (starting capacities)

### MT06_ITEM_GRANT_CAPACITIES -- `mt06_item_grant_capacities_a` (P0)

*Proves: cross-computed capacities (ammo, launcher, progressives) land correctly in the live inventory*

Build:
```
python -m worlds.metroidprime2.test.manual.mt06_item_grant_capacities --iso <vanilla.iso> --variant a
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `energy_per_tank=250`, `map_visibility=full_map_and_items`, `missile_expansions_unlock_launcher=False`, `power_bomb_expansions_unlock_power_bombs=False`, `progressive_grapple=True`, `progressive_suit=True`, `split_beam_ammo=True`, `unvisited_room_names=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Beam` x1, `Light Suit` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1
* plando: 14 placement(s):
  * `Missile Expansion` -> `Temple Grounds: Hive Chamber A - Pickup (Missile)`
  * `Missile Expansion` -> `Temple Grounds: Hive Chamber B - Pickup (Missile)`
  * `Missile Expansion` -> `Temple Grounds: Communication Area - Pickup (Missile)`
  * `Missile Launcher` -> `Temple Grounds: Hall of Honored Dead - Pickup (Seeker Launcher)`
  * `Power Bomb Expansion` -> `Temple Grounds: GFMC Compound - Pickup (Missile Launcher)`
  * `Power Bomb Expansion` -> `Temple Grounds: GFMC Compound - Pickup 2 (Missile On Ship)`
  * `Power Bomb` -> `Temple Grounds: Dynamo Chamber - Pickup (Power Bomb)`
  * `Energy Tank` -> `Temple Grounds: Storage Cavern B - Pickup (Energy Tank)`
  * `Energy Tank` -> `Temple Grounds: Windchamber Gateway - Pickup (Energy Tank)`
  * `Progressive Suit` -> `Temple Grounds: Transport to Agon Wastes - Pickup (Missile)`
  * `Progressive Suit` -> `Temple Grounds: Temple Assembly Site - Pickup (Missile)`
  * `Progressive Grapple` -> `Great Temple: Transport A Access - Pickup (Missile)`
  * `Progressive Grapple` -> `Great Temple: Temple Sanctuary - Pickup (Energy Transfer Module)`
  * `Dark Ammo Expansion` -> `Great Temple: Transport B Access - Pickup (Missile)`

Notes / derived values:
* baseline (precollected + defaults): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 45=50, item 46=50, item 58=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 1 (`Missile Expansion` at `Temple Grounds: Hive Chamber A - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 45=50, item 46=50, item 58=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 2 (`Missile Expansion` at `Temple Grounds: Hive Chamber B - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 45=50, item 46=50, item 58=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 3 (`Missile Expansion` at `Temple Grounds: Communication Area - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 45=50, item 46=50, item 58=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 4 (`Missile Launcher` at `Temple Grounds: Hall of Honored Dead - Pickup (Seeker Launcher)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 5 (`Power Bomb Expansion` at `Temple Grounds: GFMC Compound - Pickup (Missile Launcher)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 6 (`Power Bomb Expansion` at `Temple Grounds: GFMC Compound - Pickup 2 (Missile On Ship)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 7 (`Power Bomb` at `Temple Grounds: Dynamo Chamber - Pickup (Power Bomb)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 8 (`Energy Tank` at `Temple Grounds: Storage Cavern B - Pickup (Energy Tank)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=1, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 9 (`Energy Tank` at `Temple Grounds: Windchamber Gateway - Pickup (Energy Tank)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 10 (`Progressive Suit` at `Temple Grounds: Transport to Agon Wastes - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 11 (`Progressive Suit` at `Temple Grounds: Temple Assembly Site - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 12 (`Progressive Grapple` at `Great Temple: Transport A Access - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 13 (`Progressive Grapple` at `Great Temple: Temple Sanctuary - Pickup (Energy Transfer Module)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 14 (`Dark Ammo Expansion` at `Great Temple: Transport B Access - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=70, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt06_item_grant_capacities --iso <vanilla.iso> --variant a
2. Host       python MultiServer.py manual_tests/mt06_item_grant_capacities_a/mt06_item_grant_capacities_a.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt06_item_grant_capacities_a/mt06_item_grant_capacities_a.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Connect the client before collecting anything.
   **Expect:** Baseline inventory matches the table's baseline row.
2. **Do:** Collect the 14 pickups in the printed order, running `/mp2_debug_inventory` after each.
   **Expect:** After each pickup, the item amounts/capacities match the script's expected table. In this variant the Missile Expansions before the launcher raise the capacity but leave 0 usable missiles until the launcher is collected.
3. **Do:** From the server console, run `/send <player> Dark Ammo Expansion`.
   **Expect:** The corresponding ammo capacity goes up by exactly one expansion's worth, not to a stranded raw value.

Pass if:
* Variant a: expansions before the launcher grant capacity but no usable missiles until the launcher.
* Variant b: expansions unlock their weapon immediately.
* Progressive Suit grants Dark Suit then Light Suit; Progressive Grapple grants Grapple then Screw Attack.
* Energy Tanks are worth 250 each.
* The server-console expansion pushes capacity up by exactly one expansion.

If it fails, look at:
* `client/receive_items.py` (`compute_desired_capacities`/`plan_grants`)
* `client/client.py::_handle_grant_items`
* `patch_data.starting_items_config` (starting capacities)

### MT06_ITEM_GRANT_CAPACITIES -- `mt06_item_grant_capacities_b` (P0)

*Proves: cross-computed capacities (ammo, launcher, progressives) land correctly in the live inventory*

Build:
```
python -m worlds.metroidprime2.test.manual.mt06_item_grant_capacities --iso <vanilla.iso> --variant b
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `energy_per_tank=250`, `map_visibility=full_map_and_items`, `missile_expansions_unlock_launcher=True`, `power_bomb_expansions_unlock_power_bombs=True`, `progressive_grapple=True`, `progressive_suit=True`, `split_beam_ammo=False`, `unvisited_room_names=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Beam` x1, `Light Suit` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1
* plando: 14 placement(s):
  * `Missile Expansion` -> `Temple Grounds: Hive Chamber A - Pickup (Missile)`
  * `Missile Expansion` -> `Temple Grounds: Hive Chamber B - Pickup (Missile)`
  * `Missile Expansion` -> `Temple Grounds: Communication Area - Pickup (Missile)`
  * `Missile Launcher` -> `Temple Grounds: Hall of Honored Dead - Pickup (Seeker Launcher)`
  * `Power Bomb Expansion` -> `Temple Grounds: GFMC Compound - Pickup (Missile Launcher)`
  * `Power Bomb Expansion` -> `Temple Grounds: GFMC Compound - Pickup 2 (Missile On Ship)`
  * `Power Bomb` -> `Temple Grounds: Dynamo Chamber - Pickup (Power Bomb)`
  * `Energy Tank` -> `Temple Grounds: Storage Cavern B - Pickup (Energy Tank)`
  * `Energy Tank` -> `Temple Grounds: Windchamber Gateway - Pickup (Energy Tank)`
  * `Progressive Suit` -> `Temple Grounds: Transport to Agon Wastes - Pickup (Missile)`
  * `Progressive Suit` -> `Temple Grounds: Temple Assembly Site - Pickup (Missile)`
  * `Progressive Grapple` -> `Great Temple: Transport A Access - Pickup (Missile)`
  * `Progressive Grapple` -> `Great Temple: Temple Sanctuary - Pickup (Energy Transfer Module)`
  * `Beam Ammo Expansion` -> `Great Temple: Transport B Access - Pickup (Missile)`

Notes / derived values:
* baseline (precollected + defaults): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 45=50, item 46=50, item 58=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 1 (`Missile Expansion` at `Temple Grounds: Hive Chamber A - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 44=10, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 2 (`Missile Expansion` at `Temple Grounds: Hive Chamber B - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 44=15, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 3 (`Missile Expansion` at `Temple Grounds: Communication Area - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 44=20, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 4 (`Missile Launcher` at `Temple Grounds: Hall of Honored Dead - Pickup (Seeker Launcher)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 5 (`Power Bomb Expansion` at `Temple Grounds: GFMC Compound - Pickup (Missile Launcher)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 43=1, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 6 (`Power Bomb Expansion` at `Temple Grounds: GFMC Compound - Pickup 2 (Missile On Ship)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 43=2, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 7 (`Power Bomb` at `Temple Grounds: Dynamo Chamber - Pickup (Power Bomb)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 8 (`Energy Tank` at `Temple Grounds: Storage Cavern B - Pickup (Energy Tank)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=1, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 9 (`Energy Tank` at `Temple Grounds: Windchamber Gateway - Pickup (Energy Tank)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 10 (`Progressive Suit` at `Temple Grounds: Transport to Agon Wastes - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 11 (`Progressive Suit` at `Temple Grounds: Temple Assembly Site - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 12 (`Progressive Grapple` at `Great Temple: Transport A Access - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 13 (`Progressive Grapple` at `Great Temple: Temple Sanctuary - Pickup (Energy Transfer Module)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=50, item 46=50, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1
* after step 14 (`Beam Ammo Expansion` at `Great Temple: Transport B Access - Pickup (Missile)`): item 0=1, item 1=1, item 2=1, item 3=1, item 4=1, item 5=1, item 6=1, item 7=1, item 8=1, item 9=1, item 10=1, item 11=1, item 12=1, item 13=1, item 14=1, item 15=1, item 16=1, item 17=1, item 18=1, item 22=1, item 23=1, item 24=1, item 25=1, item 26=1, item 27=1, item 29=1, item 30=1, item 31=1, item 32=1, item 33=1, item 34=1, item 35=1, item 36=1, item 37=1, item 38=1, item 39=1, item 40=1, item 42=2, item 43=4, item 44=25, item 45=60, item 46=60, item 58=1, item 73=1, item 81=1, item 82=1, item 96=1, item 97=1, item 98=1, item 99=1, item 100=1, item 101=1, item 102=1, item 103=1, item 104=1, item 105=1, item 106=1

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt06_item_grant_capacities --iso <vanilla.iso> --variant b
2. Host       python MultiServer.py manual_tests/mt06_item_grant_capacities_b/mt06_item_grant_capacities_b.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt06_item_grant_capacities_b/mt06_item_grant_capacities_b.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Connect the client before collecting anything.
   **Expect:** Baseline inventory matches the table's baseline row.
2. **Do:** Collect the 14 pickups in the printed order, running `/mp2_debug_inventory` after each.
   **Expect:** After each pickup, the item amounts/capacities match the script's expected table. In this variant the Missile Expansions before the launcher raise the capacity but leave 0 usable missiles until the launcher is collected.
3. **Do:** From the server console, run `/send <player> Beam Ammo Expansion`.
   **Expect:** The corresponding ammo capacity goes up by exactly one expansion's worth, not to a stranded raw value.

Pass if:
* Variant a: expansions before the launcher grant capacity but no usable missiles until the launcher.
* Variant b: expansions unlock their weapon immediately.
* Progressive Suit grants Dark Suit then Light Suit; Progressive Grapple grants Grapple then Screw Attack.
* Energy Tanks are worth 250 each.
* The server-console expansion pushes capacity up by exactly one expansion.

If it fails, look at:
* `client/receive_items.py` (`compute_desired_capacities`/`plan_grants`)
* `client/client.py::_handle_grant_items`
* `patch_data.starting_items_config` (starting capacities)

### MT07_COUNTER_PERSISTENCE -- `mt07_counter_persistence` (P0)

*Proves: the pickup bitmask counters persist across saves and checkpoint reloads with no lost or doubled checks*

Build:
```
python -m worlds.metroidprime2.test.manual.mt07_counter_persistence --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x14, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1
* plando: 3 placement(s):
  * `Missile Expansion` -> `Temple Grounds: Hive Chamber A - Pickup (Missile)`
  * `Missile Expansion` -> `Temple Grounds: Hive Chamber B - Pickup (Missile)`
  * `Energy Tank` -> `Temple Grounds: Windchamber Gateway - Pickup (Energy Tank)`

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt07_counter_persistence --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt07_counter_persistence/mt07_counter_persistence.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt07_counter_persistence/mt07_counter_persistence.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** With the client attached, collect pickup 1.
   **Expect:** Exactly one location check is reported and the item is granted.
2. **Do:** Close the client, collect pickup 2, save at a save station, quit to the main menu, and reload the save.
   **Expect:** The game returns to the loaded save with pickup 2 collected.
3. **Do:** Reopen the client and connect.
   **Expect:** Pickup 2 is reported now (the counter survived the save), and the inventory is rebuilt from ReceivedItems with no duplicates.
4. **Do:** With the client attached, collect pickup 3, then die before saving and reload the checkpoint.
   **Expect:** No double credit and no lost check; the item is still in the inventory (it comes from the server, not the save file).

Pass if:
* Pickup 2 collected while detached is reported after reconnect.
* No check is credited twice.
* Inventory rebuilt from ReceivedItems is idempotent.

If it fails, look at:
* `client/patcher_runner.patch_iso_with_ap`'s pre-`_apply_patches` DOL writes (`powerup_should_persist` / `powerup_max` for every `constants.PICKUP_COUNTER_ITEMS` id)
* `client/versions.py` offsets for this build

### MT08_MISSED_CHECKS -- `mt08_missed_checks` (P0)

*Proves: pickups collected across several counters while the client is detached all decode back to their own locations, with nothing dropped and nothing invented*

Build:
```
python -m worlds.metroidprime2.test.manual.mt08_missed_checks --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `defense_up_damage_reduction=90`, `door_lock_rando=False`, `double_damage_multiplier=500`, `elevator_rando=False`, `energy_per_tank=500`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`, `warp_to_start=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x2, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x28, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1
* plando: 7 placement(s):
  * `Missile Expansion` -> `Temple Grounds: Hive Chamber A - Pickup (Missile)`
  * `Missile Expansion` -> `Temple Grounds: Hall of Honored Dead - Pickup (Seeker Launcher)`
  * `Missile Expansion` -> `Dark Agon Wastes: Ing Cache 4 - Pickup (Missile)`
  * `Missile Expansion` -> `Dark Agon Wastes: Junction Site - Pickup (Missile)`
  * `Missile Expansion` -> `Torvus Bog: Torvus Lagoon - Pickup (Missile)`
  * `Missile Expansion` -> `Torvus Bog: Portal Chamber (Light) - Pickup (Missile)`
  * `Missile Expansion` -> `Sanctuary Fortress: Sanctuary Entrance - Pickup (Power Bomb)`

Notes / derived values:
* one pickup per counter: [0, 30, 60, 94] -> Temple Grounds: Hive Chamber A - Pickup (Missile), Dark Agon Wastes: Ing Cache 4 - Pickup (Missile), Torvus Bog: Torvus Lagoon - Pickup (Missile), Sanctuary Fortress: Sanctuary Entrance - Pickup (Power Bomb)
* two pickups sharing counters already used above: [1, 31] -> Temple Grounds: Hall of Honored Dead - Pickup (Seeker Launcher), Dark Agon Wastes: Junction Site - Pickup (Missile)
* final single pickup: 61 -> Torvus Bog: Portal Chamber (Light) - Pickup (Missile)
* The picks come from `routes.pickups_spanning_counter_groups`, which derives the counter group from `pickup_encoding.BITS_PER_COUNTER` -- so they follow the encoding rather than being hardcoded alongside it.

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt08_missed_checks --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt08_missed_checks/mt08_missed_checks.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt08_missed_checks/mt08_missed_checks.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Connect the client and run `!missing` in the client chat.
   **Expect:** All 119 locations are listed as missing; note the 7 rooms named above.
2. **Do:** Close the client, collect the four one-per-counter pickups, then reopen/connect the client.
   **Expect:** All four locations are credited in one go and all four items are granted. The client logs no warning about stray bits and none about an index that was 'already checked server-side'. `!missing` now lists exactly the remaining 3 rooms plus everything untouched -- no location the tester never visited was credited.
3. **Do:** Close the client again, collect the two pickups that share counters with ones already collected, then reopen/connect.
   **Expect:** Both are credited (several bits on one counter decode the same way), both items are granted, and nothing else changes.
4. **Do:** With the client attached, collect the final single pickup.
   **Expect:** It credits immediately, the way an attached pickup always has -- the detached runs left the counters clean rather than poisoning the session.
5. **Do:** Optional: reload the save from before the last pickup and collect it a second time while detached, then reconnect.
   **Expect:** This is section P's one known residual failure mode (the same bit collected twice carries into its neighbour). The client must log the 'already checked server-side' warning rather than crashing or silently crediting a stranger's location -- the warning existing is the pass condition here, not the absence of the case.

Pass if:
* Every pickup collected while detached is credited to its own location, across all four counters.
* No location the tester never collected is ever credited.
* No stray-bit warning appears for an ordinary detached run.
* A normal attached pickup immediately afterwards still credits.

If it fails, look at:
* `pickup_encoding.py` (`counter_and_amount` / `decode`)
* `client/client.py::_handle_pickup_counters`
* `client/game_interface.py::consume_counters`

### MT09_REMOTE_ITEMS_RECONNECT -- `mt09_remote_items_reconnect` (P0)

*Proves: remote items arrive, queue, and re-sync idempotently across a client restart*

Build:
```
python -m worlds.metroidprime2.test.manual.mt09_remote_items_reconnect --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`, `warp_to_start=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x14, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1
* companion slots: Filler1 (Clique)

Notes / derived values:
* Driven from the **server console** (admin by virtue of being the console, so no host.yaml change is needed). Exact lines to paste:
*   `/send_multiple 5 mt09_remote_items_reconnect Missile Expansion` -- 5 items in quick succession
*   `/send mt09_remote_items_reconnect Super Missile` -- while a cutscene is playing
*   `/send mt09_remote_items_reconnect Power Bomb Expansion` -- while the pause menu is open

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt09_remote_items_reconnect --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt09_remote_items_reconnect/mt09_remote_items_reconnect.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt09_remote_items_reconnect/mt09_remote_items_reconnect.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** While in-game, paste the first `/send_multiple` line above (5 items at once).
   **Expect:** 5 HUD memos appear, correctly spaced (4s cooldown), and all 5 items are granted.
2. **Do:** Send an item while a cutscene is playing.
   **Expect:** It queues and lands after the cutscene instead of being lost.
3. **Do:** Open the pause menu and send an item.
   **Expect:** It queues and lands after the menu closes.
4. **Do:** Kill the client mid-run and restart it.
   **Expect:** It re-syncs silently and idempotently: no re-granting spam, no duplicate HUD messages.

Pass if:
* All sent items are granted exactly once.
* HUD memos are throttled to the 4s cooldown and not dropped.
* A client restart re-syncs without duplicate grants or memos.

If it fails, look at:
* `client/receive_items.plan_grants`
* `client/notification_manager.py`
* `client/client.py` (the Dolphin sync task / grant loop)

### MT10_CROSS_GAME_MODELS -- `mt10_cross_game_models` (P1)

*Proves: every distinct cross-game item model (name-matched and override) loads without crashing*

Build:
```
python -m worlds.metroidprime2.test.manual.mt10_cross_game_models --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `display_nonlocal_items=match_game`, `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x14, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1
* companion slots: MetroidPrime (Metroid Prime), MetroidZeroMission (Metroid: Zero Mission)

Notes / derived values:
* room -> (source game, item, expected MP2 model) -- one representative per distinct model:
*   `Temple Grounds: Hive Chamber A - Pickup (Missile)` <- (Metroid Prime, Boost Ball, `BoostBall`)
*   `Temple Grounds: Hall of Honored Dead - Pickup (Seeker Launcher)` <- (Metroid Prime, Charge Beam, `ChargeBeam`)
*   `Temple Grounds: Hive Chamber B - Pickup (Missile)` <- (Metroid Prime, Combat Visor, `CombatVisor`)
*   `Sky Temple Grounds: War Ritual Grounds - Pickup (Missile)` <- (Metroid Prime, Energy Tank, `EnergyTank`)
*   `Temple Grounds: Windchamber Gateway - Pickup (Energy Tank)` <- (Metroid Prime, Grapple Beam, `GrappleBeam`)
*   `Temple Grounds: Transport to Agon Wastes - Pickup (Missile)` <- (Metroid Prime, Gravity Suit, `GravityBoost`)
*   `Temple Grounds: Temple Assembly Site - Pickup (Missile)` <- (Metroid Prime, Missile Expansion, `MissileExpansionPrime1`)
*   `Temple Grounds: Grand Windchamber - Pickup (Sunburst)` <- (Metroid Prime, Missile Launcher, `MissileLauncher`)
*   `Temple Grounds: Dynamo Chamber - Pickup (Power Bomb)` <- (Metroid Prime, Morph Ball, `MorphBall`)
*   `Temple Grounds: Storage Cavern B - Pickup (Energy Tank)` <- (Metroid Prime, Morph Ball Bomb, `MorphBallBomb`)
*   `Sky Temple Grounds: Plain of Dark Worship - Pickup (Missile)` <- (Metroid Prime, Power Bomb (Main), `PowerBomb`)
*   `Great Temple: Temple Sanctuary - Pickup (Energy Transfer Module)` <- (Metroid Prime, Power Bomb Expansion, `PowerBombExpansion`)
*   `Sky Temple Grounds: Defiled Shrine - Pickup (Sky Temple Key 8)` <- (Metroid Prime, Scan Visor, `ScanVisor`)
*   `Temple Grounds: Communication Area - Pickup (Missile)` <- (Metroid Prime, Space Jump Boots, `SpaceJumpBoots`)
*   `Great Temple: Transport A Access - Pickup (Missile)` <- (Metroid Prime, Spider Ball, `SpiderBall`)
*   `Great Temple: Transport B Access - Pickup (Missile)` <- (Metroid Prime, Super Missile, `SuperMissile`)
*   `Temple Grounds: GFMC Compound - Pickup (Missile Launcher)` <- (Metroid Prime, Unlimited Missiles, `UnlimitedMissiles`)
*   `Temple Grounds: GFMC Compound - Pickup 2 (Missile On Ship)` <- (Metroid: Zero Mission, Missile Tank, `MissileExpansion`)
*   `Sky Temple Grounds: Accursed Lake - Pickup (Sky Temple Key 9)` <- (Metroid: Zero Mission, Screw Attack, `ScrewAttack`)
* models from the override table: MissileExpansionPrime1

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt10_cross_game_models --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt10_cross_game_models/mt10_cross_game_models.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt10_cross_game_models/mt10_cross_game_models.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Start the game and **walk into every room listed in the notes**, without collecting anything.
   **Expect:** No crash on area load -- crashes happen when the room loads, before you reach the pickup.
2. **Do:** Collect each listed pickup.
   **Expect:** No crash on pickup. The exact name-to-model pairing isn't being re-verified here (that's a hand-curated, name-matched table); what matters is that the underlying model loads.

Pass if:
* No crash on any room load.
* No crash on collecting any pickup.

If it fails, look at:
* Delete the offending entry from `_CROSS_GAME_ITEM_NAMES` (or `_CROSS_GAME_MODEL_OVERRIDES`, if that's what supplied the model) rather than reverting the feature -- it falls back to the generic Energy Transfer Module.
* `patch_data._pickup_appearance` / `_CROSS_GAME_MODEL_OVERRIDES`

### MT11_DEATH_LINK -- `mt11_death_link` (P1)

*Proves: Death Link works in both directions in a live game without a death loop*

Build:
```
python -m worlds.metroidprime2.test.manual.mt11_death_link --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `death_link=True`, `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`
* start inventory: `Amber Translator` x1, `Annihilator Beam` x1, `Beam Ammo Expansion` x1, `Boost Ball` x1, `Cannon Ball` x1, `Charge Beam` x1, `Cobalt Translator` x1, `Combat Visor` x1, `Dark Agon Key 1` x1, `Dark Agon Key 2` x1, `Dark Agon Key 3` x1, `Dark Ammo Expansion` x10, `Dark Beam` x1, `Dark Suit` x1, `Dark Torvus Key 1` x1, `Dark Torvus Key 2` x1, `Dark Torvus Key 3` x1, `Dark Visor` x1, `Darkburst` x1, `Double Damage` x1, `Echo Visor` x1, `Emerald Translator` x1, `Energy Tank` x14, `Grapple Beam` x1, `Gravity Boost` x1, `Ing Hive Key 1` x1, `Ing Hive Key 2` x1, `Ing Hive Key 3` x1, `Light Ammo Expansion` x10, `Light Beam` x1, `Light Suit` x1, `Missile Expansion` x33, `Missile Launcher` x1, `Morph Ball` x1, `Morph Ball Bomb` x1, `Power Beam` x1, `Power Bomb` x1, `Power Bomb Expansion` x8, `Progressive Grapple` x2, `Progressive Suit` x2, `Scan Visor` x1, `Screw Attack` x1, `Seeker Launcher` x1, `Sky Temple Key 1` x1, `Sky Temple Key 2` x1, `Sky Temple Key 3` x1, `Sky Temple Key 4` x1, `Sky Temple Key 5` x1, `Sky Temple Key 6` x1, `Sky Temple Key 7` x1, `Sky Temple Key 8` x1, `Sky Temple Key 9` x1, `Sonic Boom` x1, `Space Jump Boots` x1, `Spider Ball` x1, `Sunburst` x1, `Super Missile` x1, `Unlimited Beam Ammo` x1, `Unlimited Missiles` x1, `Varia Suit` x1, `Violet Translator` x1
* companion slots: DeathLinkPartner (Metroid Prime 2: Echoes)

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt11_death_link --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt11_death_link/mt11_death_link.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt11_death_link/mt11_death_link.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** In the client chat, run `/test_deathlink outgoing`.
   **Expect:** The other slot dies.
2. **Do:** Run `/test_deathlink incoming`.
   **Expect:** You die in-game.
3. **Do:** Cause a *real* death (stand in Dark Aether without a suit).
   **Expect:** The other slot dies. No death loop: receiving a death while already dead or during the death animation does not re-broadcast.

Pass if:
* Outgoing and incoming test deaths both work.
* A real in-game death is sent exactly once.
* No death loop (the incoming death is not echoed back out).

If it fails, look at:
* `client/death_link.py::death_link_check`
* `client/client.py::on_deathlink` / `_handle_check_deathlink`

### MT12_SESSION_LIFECYCLE -- `mt12_session_lifecycle` (P2)

*Proves: menu/new game/load/wrong-seed/reconnect/export_iso all behave*

Build:
```
python -m worlds.metroidprime2.test.manual.mt12_session_lifecycle --iso <vanilla.iso>
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt12_session_lifecycle --iso <vanilla.iso>
2. Host       python MultiServer.py manual_tests/mt12_session_lifecycle/mt12_session_lifecycle.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt12_session_lifecycle/mt12_session_lifecycle.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Connect the client while Dolphin is at the main menu.
   **Expect:** The client reports `IN_MENU`.
2. **Do:** Start a New Game.
   **Expect:** The client reports `IN_GAME`.
3. **Do:** Load the *other* seed's ISO (`--variant other`) with this client still connected.
   **Expect:** The client reports `WRONG_SEED`.
4. **Do:** Stop and restart emulation, then run `/reconnect`.
   **Expect:** The client reconnects and reports the current state.
5. **Do:** Run `/export_iso` with the game closed.
   **Expect:** The existing ISO is deleted and re-patched.
6. **Do:** Run `/export_iso` with the game open.
   **Expect:** The client refuses with an error.
7. **Do:** Run `/status`, `/test_hud hello`, and `/mp2_debug_inventory`.
   **Expect:** `/status` prints the connection state; `/test_hud` shows a HUD memo; the inventory dump lists non-empty slots.

Pass if:
* Every command behaves as listed.
* Wrong-seed detection fires.
* `/export_iso` refuses while connected and re-patches while disconnected.

If it fails, look at:
* `client/client.py` (`MetroidPrime2CommandProcessor`, `update_connection_status`)
* `client/game_interface.py` (`get_connection_state`)

### MT12_SESSION_LIFECYCLE -- `mt12_session_lifecycle_other` (P2)

*Proves: menu/new game/load/wrong-seed/reconnect/export_iso all behave*

Build:
```
python -m worlds.metroidprime2.test.manual.mt12_session_lifecycle --iso <vanilla.iso> --variant other
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt12_session_lifecycle --iso <vanilla.iso> --variant other
2. Host       python MultiServer.py manual_tests/mt12_session_lifecycle_other/mt12_session_lifecycle_other.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt12_session_lifecycle_other/mt12_session_lifecycle_other.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Connect the client while Dolphin is at the main menu.
   **Expect:** The client reports `IN_MENU`.
2. **Do:** Start a New Game.
   **Expect:** The client reports `IN_GAME`.
3. **Do:** Load the *other* seed's ISO (`--variant other`) with this client still connected.
   **Expect:** The client reports `WRONG_SEED`.
4. **Do:** Stop and restart emulation, then run `/reconnect`.
   **Expect:** The client reconnects and reports the current state.
5. **Do:** Run `/export_iso` with the game closed.
   **Expect:** The existing ISO is deleted and re-patched.
6. **Do:** Run `/export_iso` with the game open.
   **Expect:** The client refuses with an error.
7. **Do:** Run `/status`, `/test_hud hello`, and `/mp2_debug_inventory`.
   **Expect:** `/status` prints the connection state; `/test_hud` shows a HUD memo; the inventory dump lists non-empty slots.

Pass if:
* Every command behaves as listed.
* Wrong-seed detection fires.
* `/export_iso` refuses while connected and re-patches while disconnected.

If it fails, look at:
* `client/client.py` (`MetroidPrime2CommandProcessor`, `update_connection_status`)
* `client/game_interface.py` (`get_connection_state`)

### MT13_PAL_PARITY -- `mt13_pal_parity` (P1)

*Proves: MT01/MT03/MT04/MT07 pass identically on a PAL ISO (different DOL addresses)*

Build:
```
python -m worlds.metroidprime2.test.manual.mt13_pal_parity --iso <vanilla.iso> --pal
```

What the build contains:
* starting room: `vanilla (Temple Grounds/Landing Site/Save Station)`
* options: `door_lock_rando=False`, `elevator_rando=False`, `map_visibility=full_map_and_items`, `portal_rando=False`, `translator_gate_rando=vanilla`, `unvisited_room_names=True`

Notes / derived values:
* This is not a separate build: it is a checklist to re-run the per-build-address tests with `--pal`.
* Any difference from the NTSC run means `client/versions.py` (or `tools/find_warp_addresses.py`) needs a PAL-specific fix.

Run:
```
1. Build      python -m worlds.metroidprime2.test.manual.mt13_pal_parity --iso <vanilla.iso> --pal
2. Host       python MultiServer.py manual_tests/mt13_pal_parity/mt13_pal_parity.zip
3. Connect    python Launcher.py "Metroid Prime 2 Client" manual_tests/mt13_pal_parity/mt13_pal_parity.apmp2 <vanilla.iso>
               (the client reuses the already-patched ISO instead of re-patching)
```

Steps:
1. **Do:** Run MT01 (`mt01_smoke_vanilla`) with `--pal`.
   **Expect:** It passes exactly as on NTSC.
2. **Do:** Run MT03 (`mt03_goal_area_poke`) with `--pal`.
   **Expect:** It passes exactly as on NTSC.
3. **Do:** Run MT04 (`mt04_warp_to_start_save_station`) with `--pal`.
   **Expect:** It passes exactly as on NTSC.
4. **Do:** Run MT07 (`mt07_counter_persistence`) with `--pal`.
   **Expect:** It passes exactly as on NTSC.

Pass if:
* Every re-run test passes on PAL.
* No PAL-specific crash or address mismatch.

If it fails, look at:
* `client/versions.py` (`PAL` address table)
* `tools/find_warp_addresses.py` (re-derive the warp hook for PAL)
* `client/warp_patch.py`

## Suggested runs

* **Smoke** -- `MT01`, `MT03` (boot + goal detection).
* **Pre-release** -- every P0 and P1 test.
* **Everything** -- P0 + P1 + P2.

`MT10` may crash the game by design (unverified cross-game models); run it
last in a session and note which entry crashed.
